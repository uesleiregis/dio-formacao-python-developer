cores = ("vermelho", "azul", "verde", "azul")

cores.count("vermelhor")
cores.count("azul")
cores.count('verde')

a=f"""
{
cores.count("vermelhor"),
cores.count("azul"),
cores.count('verde'),
}
"""
print(a)